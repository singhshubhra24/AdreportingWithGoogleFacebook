"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const lodash_1 = require("lodash");
class GoogleAdsError extends Error {
    constructor({ message, status, details, code }) {
        const detailed_message = lodash_1.get(details, '[0].errors[0].message');
        message = detailed_message ? `${message} ${detailed_message}` : message;
        super(message);
        this.error_name = message;
        this.details = details;
        this.status = status;
        this.http_code = code;
        this.trigger = lodash_1.get(details, '[0].errors[0].trigger.stringValue');
        this.location = lodash_1.get(details, '[0].errors[0].location.fieldPathElements');
        Error.captureStackTrace(this, this.constructor);
    }
}
exports.default = GoogleAdsError;
