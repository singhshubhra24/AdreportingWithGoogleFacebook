"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const Entity_1 = __importDefault(require("./Entity"));
const constants_1 = require("../constants");
class CampaignBudgets extends Entity_1.default {
    constructor(http_controller) {
        super(http_controller, constants_1.ENDPOINTS.campaign_budgets, constants_1.RESOURCE_NAMES.campaign_budget);
    }
    create(config) {
        if (Array.isArray(config)) {
            for (const entry of config) {
                if (entry.explicitly_shared && !entry.name) {
                    throw new Error('Field { name } is required for explicitly shared campaign budgets');
                }
            }
        }
        else if (config.explicitly_shared && !config.name) {
            throw new Error('Field { name } is required for explicitly shared campaign budgets');
        }
        return super.create(config);
    }
    retrieve(campaign_budget_id) {
        return super.retrieve(campaign_budget_id);
    }
}
exports.default = CampaignBudgets;
