import { ListConfig, EntityUpdateConfig, NewEntityConfig } from '../types/Entity';
import { HttpController } from '../types/Http';
export default class Entity {
    private http_controller;
    private endpoint;
    private resource;
    constructor(http_controller: HttpController, endpoint: string, resource: string);
    retrieve(entity_id: string | number): Promise<any>;
    create(config: NewEntityConfig | NewEntityConfig[]): Promise<any>;
    list(config?: ListConfig): Promise<any>;
    update(config: EntityUpdateConfig | EntityUpdateConfig[]): Promise<any>;
    delete(entity_id: string | number): Promise<any>;
}
